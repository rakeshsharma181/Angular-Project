import { DescendingPipe } from './descending.pipe';

describe('DescendingPipe', () => {
  it('create an instance', () => {
    const pipe = new DescendingPipe();
    expect(pipe).toBeTruthy();
  });
});
